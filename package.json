{
  "name"       : "fierytouch",
  "version"    : "0.0.1",
  "description": "FieryTouch's User Interface.",
  "author"     : "Dimitri Racordon",
  "devDependencies": {
    "babel-preset-es2015" : "latest",
    "babel-preset-react"  : "latest",
    "babel-preset-stage-2": "latest",
    "babelify"            : "latest",
    "babel-register"      : "latest",
    "browserify"          : "latest",
    "uglify-js"           : "latest",
    "watchify"            : "latest"
  },
  "dependencies": {
    "bluebird"            : "latest",
    "cytoscape"           : "latest",
    "cytoscape"           : "latest",
    "cytoscape-euler"     : "latest",
    "immutable"           : "latest",
    "redux"               : "latest"
  },
  "babel": {
    "presets": ["es2015", "react", "stage-2"]
  },
  "browserify": {
    "transform": ["babelify"]
  },
  "scripts": {
    "clean"      : "rm -f ./static/bundle.*",
    "build-debug": "browserify src/main.js -o ./static/bundle.js -v -d",
    "build"      : "browserify src/main.js | uglifyjs -cm > ./static/bundle.js",
    "watch"      : "watchify src/main.js -o ./static/bundle.js -v -d"
  }
}
